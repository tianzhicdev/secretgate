name: secrets
on:
  push:
  pull_request:
jobs:
  secretgate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: behavior matrix (allow-annotation is same-line-only)
        run: python3 scripts/sameline-matrix.py
      - uses: tianzhicdev/secretgate-action@v1.2.2
